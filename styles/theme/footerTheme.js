import { createTheme } from "@mui/material/styles";

const footerTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#604583",
      light: "#7c63a0",
      dark: "#b5abd4"
    }
  },
});

export default footerTheme;
